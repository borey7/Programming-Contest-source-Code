#include<bits/stdc++.h>

using namespace std;

string createString(int N, int K)
{
	string ans = "";
	if(k==0)
	{
		for(int i=0;i<N;i++)
			ans.concat("B");
			
	}
	else if(K > N/2 * (N - N/2)) ans = "";
	else
	{
		
	}
    
    return ans;
}

int main()
{    
    int N, K;
    cin>>N>>K;    	
    cout<<createString(N, K)<<endl;
}
